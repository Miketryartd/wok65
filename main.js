function mobileMenu(){
    var nlm = document.getElementById('nlm');

    if (nlm.style.visibility === 'visible'){
        nlm.style.visibility = 'hidden';
    } else {
        nlm.style.visibility = 'visible';
    }
}